# URL of the merged AarogyaFlow FastAPI backend
VITE_API_BASE_URL=http://localhost:8000

# Force offline mock services (1) or auto-fallback to backend (0)
VITE_USE_MOCKS=0

# Force the voice pipeline into LOW_CONFIDENCE for demo scenario D
VITE_FORCE_LOW_CONFIDENCE=0